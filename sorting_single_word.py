def sortword(word):
    st = [char for char in word]
    sp =sorted(word)
    print(sp)

if __name__=='__main__':
    word = 'james'
    sortword(word)
#sorted works with sequence dict snd set and created a new variable other than sort wich dont
